<h1>Blog Articles</h1>

<ul>
  {{#each blogs}}
    <li>
      <h2>{{this.title}}</h2>
      <p>{{this.description}}</p>
      <p>{{this.body}}</p>
      <p>{{this.instagram}}</p>
      <button>Submit Your Blog!</button>
    </li>
  {{/each}}
</ul>